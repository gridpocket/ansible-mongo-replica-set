use admin
db.auth("siteUserAdmin", "{{ db_user_admin_password }}");
use {{ db_name }}
db.createUser({
    user: "{{ db_user_name }}",
    pwd: "{{ db_user_password }}",
    roles:
    [
      {
        role: "dbOwner",
        db: "{{ db_name }}"
      }
    ]
});
